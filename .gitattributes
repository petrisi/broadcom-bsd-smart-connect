# Shell scripts run on the router. CRLF breaks the shebang line outright
# ("bad interpreter"), so force LF regardless of the checkout platform.
*.sh   text eol=lf
*.md   text eol=lf
*.html text eol=lf
LICENSE text eol=lf
.gitignore text eol=lf
